import { Box } from "@mui/material";
import { Header } from "../../components/Header";

export const Dashboard = () => {
  return (
    <Box m={2}>
      <Header title="DASHBOARD" subtitle="all info in once!"></Header>
      <div>Dashboard</div>
    </Box>
  );
};
